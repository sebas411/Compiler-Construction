class Main {
  x : Int;
  y : Bool;
  
  main() : Int {
    if y then 
      x <- x + 1;
    else
      x <- x - 1;
    fi

    if x = 5 then
      y <- false;
    fi

    while x < 10 loop
      x <- x + 1;
    pool
  };
};
